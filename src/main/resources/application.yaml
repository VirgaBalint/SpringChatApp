#PASS=turandot;URL=jdbc:mysql://95.111.248.108:16603/pm2201_VB;USER=VB
spring:
  datasource:
    url: ${URL}
    username: ${NAME}
    password: ${PASS}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  address: localhost
  port: 8080